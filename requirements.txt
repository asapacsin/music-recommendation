# Core (CLAP train/eval, FAISS, audio)
numpy>=1.26.4,<2.1
faiss-cpu>=1.9,<1.14
torch==2.9.0
torchvision==0.24.0
laion-clap
tqdm>=4.66.4
librosa>=0.10.2.post1

# init_model / text_processing (load_map, descriptions)
mutagen>=1.47
transformers>=4.36.0
ftfy
regex
# OpenAI CLAP text path uses `import clip`
git+https://github.com/openai/CLIP.git

# Config + metadata LLM extraction
python-dotenv>=1.0.1
openai>=2.31.0

# app/utils/translator.py
googletrans==4.0.2

# Optional: legacy recommend path (app/music_processing.py)
# torchopenl3

# Optional: video conversion only (app/converters/movie_convert.py)
# moviepy==1.0.3

# System binary (NOT a pip package): install ffmpeg on PATH for
# music_split_to_15s.py and music_convert_to_mp3.py
# e.g. conda install -c conda-forge ffmpeg
