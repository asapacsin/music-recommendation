# RUNLOG — `<run_id>`

Append-only. Newest entries at the **bottom**.

Format per entry:

---

## Entry — `<ISO8601 or date>`

- **Git SHA** (optional): `git rev-parse --short HEAD`
- **Actor**: human / Cursor agent
- **Command**:

```bash

```

- **Exit code**:
- **Outputs created / updated**:
- **Notes**:

---

## Entry — template (delete after first real entry)

*(Copy the block above for each command batch.)*
